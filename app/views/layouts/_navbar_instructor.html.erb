<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4 py-2">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/">
      <div class="bg-primary text-white d-flex align-items-center justify-content-center fw-bold" style="width: 32px; height: 32px; border-radius: 6px;">
        C
      </div>
      Codehaus
    </a>

    <!-- Cohort Pill -->
    <% if current_user&.cohort %>
      <div class="ms-3 bg-secondary bg-opacity-25 rounded-pill px-3 py-1 text-white border border-secondary border-opacity-50">
        <div class="fw-bold" style="font-size: 0.85rem;"><%= current_user.cohort.name %></div>
        <div class="text-white-50" style="font-size: 0.7rem; line-height: 1;"><%= current_user.cohort.city %></div>
      </div>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-theme="dark" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse " id="navbarNav">
      <!-- Nav Links -->
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 gap-3 fw-medium">
        <%= content_tag :li, class: "nav-item" do %>
          <%= link_to "Instructor Portal", instructor_dashboard_path, class: "nav-link #{'active text-primary border-bottom border-primary border-3 pb-1' if current_page?(instructor_dashboard_path)}" %>
        <% end %>
        <%= content_tag :li, class: "nav-item" do %>
          <%= link_to "Lectures", instructor_lectures_path, class: "nav-link #{'active text-primary border-bottom border-primary border-3 pb-1' if controller_name == 'lectures'}" %>
        <% end %>
        <%= content_tag :li, class: "nav-item" do %>
          <%= link_to "Class Sessions", instructor_class_sessions_path, class: "nav-link #{'active text-primary border-bottom border-primary border-3 pb-1' if ['class_sessions', 'attendances'].include?(controller_name)}" %>
        <% end %>
        <%= content_tag :li, class: "nav-item" do %>
          <%= link_to "Challenges", instructor_challenges_path, class: "nav-link #{'active text-primary border-bottom border-primary border-3 pb-1' if ['challenges', 'submissions'].include?(controller_name)}" %>
        <% end %>
      </ul>

      <!-- User Menu -->
      <ul class="navbar-nav ms-4 align-items-center">
        <% if user_signed_in? %>
          <li class="nav-item me-3 text-white-50">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path><path d="M13.73 21a2 2 0 0 1-3.46 0"></path></svg>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link p-0 d-flex align-items-center dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= image_tag current_user.avatar_url || "https://i.pravatar.cc/150", class: "rounded-circle border border-primary border-2", style: "width: 40px; height: 40px; object-fit: cover;" %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow border-0" style="min-width: 200px;">
              <li class="px-3 py-2 text-muted fw-bold border-bottom">
                <%= current_user.first_name %> <%= current_user.last_name %>
                <div class="fw-normal" style="font-size: 0.8rem;"><%= current_user.role.titleize %></div>
              </li>
              <li><%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item mt-2" %></li>
              <li><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item text-danger fw-bold" %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-light btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
